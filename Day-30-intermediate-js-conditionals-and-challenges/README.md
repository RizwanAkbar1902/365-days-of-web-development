# Day 30: Intermediate JavaScript (Conditionals & Logic)

Completed Section 15 modules focused on pseudo-random number generation, strict equality, conditional branching, and multi-step algorithmic challenges.

### Concepts Covered:
* **Random Numbers:** Generated dynamic integer ranges using `Math.random()` and `Math.floor()`.
* **Comparators & Equality:** Practiced strict comparison operators (`===`, `!==`) versus loose equality.
* **Logical Combinations:** Combined boolean expressions using logical AND (`&&`) and OR (`||`).
* **Advanced BMI Calculator:** Extended basic arithmetic to return contextual health interpretations based on calculated BMI brackets.
* **Leap Year Challenge:** Implemented nested conditional checks applying the 4/100/400 leap year divisibility rule.

### Execution:
* Run in terminal: `node script.js`
* Or open `index.html` and inspect the browser console.